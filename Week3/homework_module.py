def squarefootage(length, width):
    area = (length*width)
    return(area)
    #a function to calcualte the square footage of a house needs the house's length and width

def circumference(radius):
    area=(radius)
    return(area)

    from math import pi
    return 2*pi*r

    #a funciton to calculate the circumeference of a circle of a circle based on the circle's radiusm
    #needs radius as input

    #he's going to use the math package's pie attribute a built in math module the pie atribute